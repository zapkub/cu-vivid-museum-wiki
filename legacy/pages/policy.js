
import React from 'react'
import withApp from '../lib/withApp'
import StaticPage from '../containers/StaticPage'

export default withApp('Policy and Privacy')(() => <StaticPage source='/policy-md' />)
